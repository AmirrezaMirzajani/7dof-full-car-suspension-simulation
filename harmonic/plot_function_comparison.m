function plot_function_comparison(x, Y, titles, Vx0, j, colors, lineStyles)
    numPlots = length(Y);  % ????? ????????
    fontSize = 12; 
    fontName = 'Arial';

    % ????? ????? ????? ? ??????? ???? subplot
    if numPlots == 1
        rows = 1; cols = 1;
    elseif numPlots == 2
        rows = 2; cols = 1; % ?? ?????? ?? ?? ????
    elseif numPlots == 3
        rows = 2; cols = 2; % ?? ?????? ?? ???? 2x2
    elseif numPlots == 4
        rows = 2; cols = 2; % ???? ?????? ?? ???? 2x2
    else
        rows = 2; cols = 2; % ?? ??? ??????? 2x2
    end

    % ??? ??? ??? ??? ???? ????? ???
%     if hold_on_flag == 1
%         figure(fig_num);  % ??????? ?? ????? ???? ???? figure
%         clf;  % ??? ???? ?????? ???? ?? ???? ????? ????
%     end

    for i = 1:numPlots
        subplot(rows, cols, i);  % ????? ?????? subplot
        hold on;

        % ??? ?????? ?? ??? ? ??? ?? ???? ?? ????
        plot(x, Y{i}, 'Color', colors{j}, 'LineStyle', lineStyles{j}, 'LineWidth', 1.5);
        grid on;
        
        % ????? ???? ?????? ???????? ? ?????? ????
        title(['\textbf{' titles{i} '}'], 'Interpreter', 'latex', 'FontSize', fontSize, 'FontName', fontName);
        xlabel('\textbf{Time (s)}', 'Interpreter', 'latex', 'FontSize', fontSize, 'FontName', fontName);
        ylabel('\textbf{Value}', 'Interpreter', 'latex', 'FontSize', fontSize, 'FontName', fontName);
        
    end
    hold off;

end
